{
  "name": "blog-api",
  "version": "1.0.0",
  "description": "Api blog",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && nodejs ./src/index.js NODE_ENV=prd",
    "start:dev": "nodemon ./src/index.js --exec babel-node --presets es2015,stage-2 NODE_ENV=dev",
    "build": "babel <dirSource> -d <dirDestine>",
    "test:unit": "mocha --no-colors --require babel-core/register --require babel-polyfill ./test/unit/**/*.spec.js",
    "test:integration": "mocha --no-colors --require babel-core/register --require babel-polyfill ./test/integration/**/*.spec.js",
    "test": "NODE_ENV=test npm run test:unit && npm run test:integration"
  },
  "keywords": [
    "blog",
    "api"
  ],
  "author": "Tiago Rosa da costa",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^2.0.1",
    "body-parser": "^1.18.3",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "jsonwebtoken": "^8.3.0",
    "mongoose": "^5.1.2",
    "morgan": "^1.9.0",
    "winston": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "sinon": "^6.0.0",
    "supertest": "^3.1.0"
  }
}
